---
layout: single
title: "코드숨 6주차 과제1: 레스토랑 상세 페이지 구현하기"
# categories: Git
categories:
  - React # HTML CSS JavaScript Server Algorithm Wecodes Programmers CS Github Blog
tag: [리액트 기초, 라우터] #tag는 여러개 가능함
toc: true #table of content 기능!
toc_sticky: true
author_profile: true #blog 글안에서는 author_profile이 따라다니지 않도록 설정함
date: 2022-09-15T14:30:00+09:00
# sidebar:
# nav: "docs" #네비게이션에 있는 docs를 의미함
---
<style>
.crimson {
  color: crimson;
  font-weight: bold;
}

.mediumblue {
  color: mediumblue;
  font-weight: bold;
}

.forestgreen {
  color: forestgreen;
  font-weight: bold;
}
</style>

# 코드숨 6주차 과제1: 레스토랑 상세 페이지 구현하기
# commit 기록
## (1) 리액트 라우터 사용을 위한 기본 설정
```jsx
// webpack.config.js
const path = require('path');
module.exports = {
  entry: path.resolve(__dirname, 'src/index.jsx'),
  module: {
    rules: [
      {
        test: /\.jsx?$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  devServer: { // add ~
    historyApiFallback: {
      index: 'index.html',
    },
  }, // ~ add
};
```

## (2) App 컴포넌트에서 RestaurantsPage 컴포넌트를 연결
```jsx
// RestaurantsPage.test.jsx
import { render } from '@testing-library/react'; // add ~

import { useDispatch, useSelector } from 'react-redux';

import RestaurantsPage from './RestaurantsPage';

test('RestaurantsPage', () => {
  const dispatch = jest.fn();

  useDispatch.mockImplementation(() => dispatch);

  useSelector.mockImplementation((selector) => selector({
    regions: [
      { id: 1, name: '서울' },
    ],
    categories: [
      { id: 1, name: '한식' },
    ],
    restaurants: [
      { id: 1, name: '마법사주방' },
    ],
  }));

  const { queryByText } = render((
    <RestaurantsPage />
  ));

  expect(dispatch).toBeCalled();

  expect(queryByText('서울')).not.toBeNull();
  expect(queryByText('한식')).not.toBeNull();
}); // ~ add
```

```jsx
// RestaurantsPage.jsx
import { useEffect } from 'react'; // add ~ 

import { useDispatch } from 'react-redux';

import RegionsContainer from './RegionsContainer';
import CategoriesContainer from './CategoriesContainer';
import RestaurantsContainer from './RestaurantsContainer';

import {
  loadInitialData,
} from './actions';

export default function RestaurantsPage() {
  const dispatch = useDispatch();

  useEffect(() => {
    dispatch(loadInitialData(dispatch));
  }, []);

  return (
    <div>
      <RegionsContainer />
      <CategoriesContainer />
      <RestaurantsContainer />
    </div>
  );
} // ~ add
```

```jsx
// App.jsx
import { useEffect } from 'react'; // delete ~

import { useDispatch } from 'react-redux';

import RegionsContainer from './RegionsContainer';
import CategoriesContainer from './CategoriesContainer';
import RestaurantsContainer from './RestaurantsContainer';

import {
  loadInitialData,
} from './actions';

// 0. 지역, 분류 목록을 얻기
// 1. 지역 선택 - Regions <- API
// 2. 분류 선택 - Category <- API
// 3. 식당 목록 - Restaurants <- API (with region, category) // ~ delete
import RestaurantsPage from './RestaurantsPage'; // add

export default function App() {
  const dispatch = useDispatch(); // delete ~

  useEffect(() => {
    dispatch(loadInitialData(dispatch));
  }, []); // ~ delete

  return (
    <div> // delete ~
      <RegionsContainer />
      <CategoriesContainer />
      <RestaurantsContainer /> 
    </div> // ~ delete
    <RestaurantsPage /> // add
  );
}
```

```jsx
// App.test.jsx
import { render } from '@testing-library/react';

import { useDispatch, useSelector } from 'react-redux'; // delete
import { useSelector } from 'react-redux'; // add

import App from './App';

test('App', () => {
  const dispatch = jest.fn(); // delete ~

  useDispatch.mockImplementation(() => dispatch); // ~ delete

  useSelector.mockImplementation((selector) => selector({
    regions: [ // delete ~
      { id: 1, name: '서울' },
    ],
    categories: [
      { id: 1, name: '한식' },
    ],
    restaurants: [
      { id: 1, name: '마법사주방' },
    ], // ~ delete
    regions: [], // add ~
    categories: [],
    restaurants: [], // ~ add
  }));

  const { queryByText } = render(( // delete ~
    <App />
  ));

  expect(dispatch).toBeCalled();

  expect(queryByText('서울')).not.toBeNull();
  expect(queryByText('한식')).not.toBeNull(); // ~ delete
  render(<App />); // add
});
```

## (3) App 컴포넌트에서 pathname이 default일 때 Home이 되도록 구현
```jsx
// App.jsx
import RestaurantsPage from './RestaurantsPage';

export default function App() {
  const { location: { pathname } } = window; // add ~

  if (pathname === '/') {
    return (
      <p>Home</p>
    );
  } // ~ add

  return (
    <RestaurantsPage />
  );
}
```

## (4) pathname와 객체 대괄호 표기법을 활용한 기본적인 라우팅을 구현
```jsx
// App.jsx
import RestaurantsPage from './RestaurantsPage';

function HomePage() { // add ~
  return (
    <p>Home</p>
  );
}

function AboutPage() {
  return (
    <p>About...</p>
  );
}

function NotFoundPage() {
  return (
    <p>NotFoundPage</p>
  );
} // ~ add

export default function App() {
  const { location: { pathname } } = window;

  if (pathname === '/') { // delete ~
    return (
      <p>Home</p>
    );
  } // ~ delete
  const MyComponent = { // add ~
    '/': HomePage,
    '/about': AboutPage,
    '/restaurants': RestaurantsPage,
  }[pathname] || NotFoundPage; // ~ add

  return (
    <RestaurantsPage /> // delete
    <MyComponent /> // add
  );
}
```

## (5) App 컴포넌트의 HomePage 함수의 기본구조를 추가
```jsx
// App.jsx
import RestaurantsPage from './RestaurantsPage';

function HomePage() {
  return (
    <p>Home</p> // delete
    <div> // add ~
      <h1>Home</h1>
      <ul>
        <li><a href="/about">About</a></li>
        <li><a href="/restaurants">Restaurants</a></li>
      </ul>
    </div> // ~ add
  );
}

function AboutPage() {
  return (
    <p>About...</p>
  );
}
function NotFoundPage() {
  return (
    <p>NotFoundPage</p>
  );
}
export default function App() {
  const { location: { pathname } } = window;
  const MyComponent = {
    '/': HomePage,
    '/about': AboutPage,
    '/restaurants': RestaurantsPage,
  }[pathname] || NotFoundPage;
  return (
    <MyComponent />
  );
}
```

## (6) App 컴포넌트에서 HomePage를 컴포넌트로 분리
```jsx
// HomePage.test.jsx
import { render } from '@testing-library/react'; // add ~

import HomePage from './HomePage';

test('HomePage', () => {
  render(<HomePage />);
}); // ~ add
```

```jsx
// HomePage.jsx
export default function HomePage() {
  return (
    <div>
      <h1>Home</h1>
      <ul>
        <li><a href="/about">About</a></li>
        <li><a href="/restaurants">Restaurants</a></li>
      </ul>
    </div>
  );
}
```

```jsx
// App.jsx
import HomePage from './HomePage';
import RestaurantsPage from './RestaurantsPage';

function HomePage() { // delete ~
  return (
    <div>
      <h1>Home</h1>
      <ul>
        <li><a href="/about">About</a></li>
        <li><a href="/restaurants">Restaurants</a></li>
      </ul>
    </div>
  );
} // ~ delete

function AboutPage() {
  return (
    <p>About...</p>
  );
}

function NotFoundPage() {
  return (
    <p>NotFoundPage</p>
  );
}

export default function App() {
  const { location: { pathname } } = window;

  const MyComponent = {
    '/': HomePage,
    '/about': AboutPage,
    '/restaurants': RestaurantsPage,
  }[pathname] || NotFoundPage;

  return (
    <MyComponent />
  );
}
```

## (7) App 컴포넌트에서 AboutPage를 컴포넌트로 분리
```jsx
// AboutPage.test.jsx
import { render } from '@testing-library/react';

import AboutPage from './AboutPage';

test('AboutPage', () => {
  render(<AboutPage />);
});
```

```jsx
// AboutPage.jsx
export default function AboutPage() {
  return (
    <p>About...</p>
  );
}
```

```jsx
// App.jsx
import HomePage from './HomePage';
import AboutPage from './AboutPage'; // add
import RestaurantsPage from './RestaurantsPage';

function AboutPage() { // delete ~
  return (
    <p>About...</p>
  );
} // ~ delete

function NotFoundPage() {
  return (
    <p>NotFoundPage</p>
  );
}
export default function App() {
  const { location: { pathname } } = window;

  const MyComponent = {
    '/': HomePage,
    '/about': AboutPage,
    '/restaurants': RestaurantsPage,
  }[pathname] || NotFoundPage;

  return (
    <MyComponent />
  );
}
```

## (8) App 컴포넌트에서 NotFoundPage를 컴포넌트로 분리
```jsx
// NotFoundPage.test.jsx
import { render } from '@testing-library/react';

import NotFoundPage from './NotFoundPage';

test('NotFoundPage', () => {
  render(<NotFoundPage />);
});
```

```jsx
// NotFoundPage.jsx
export default function NotFoundPage() {
  return (
    <p>Not Found Page</p>
  );
}
```

```jsx
// App.jsx
import HomePage from './HomePage';
import AboutPage from './AboutPage';
import NotFoundPage from './NotFoundPage'; // add
import RestaurantsPage from './RestaurantsPage';

function NotFoundPage() { // delete ~
  return (
    <p>NotFoundPage</p>
  );
} // ~ delete

export default function App() {
  const { location: { pathname } } = window;

  const MyComponent = {
    '/': HomePage,
    '/about': AboutPage,
    '/restaurants': RestaurantsPage,
  }[pathname] || NotFoundPage;
  return (
    <MyComponent />
  );
}
```

## (9) react-router-dom을 사용하여 App 컴포넌트의 라우팅 초기 설정
```jsx
// App.jsx
import { // add ~
  BrowserRouter,
  Routes,
  Route,
} from 'react-router-dom'; // ~ add

import HomePage from './HomePage';
import AboutPage from './AboutPage';
import NotFoundPage from './NotFoundPage';
import RestaurantsPage from './RestaurantsPage';

export default function App() {
  const { location: { pathname } } = window; // delete ~

  const MyComponent = {
    '/': HomePage,
    '/about': AboutPage,
    '/restaurants': RestaurantsPage,
  }[pathname] || NotFoundPage; // ~ delete

  return (
    <MyComponent /> // delete
    <BrowserRouter> // add ~
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/about" element={<AboutPage />} />
        <Route path="/restaurants" element={<RestaurantsPage />} />
        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </BrowserRouter> // ~ add
  );
}
```

## (10) App 컴포넌트의 path가 '/', '/about'일 경우의 액션을 테스트 및 구현
```jsx
// App.test.jsx
import { // add ~
  MemoryRouter,
} from 'react-router-dom'; // ~ add

import { render } from '@testing-library/react';

import { useSelector } from 'react-redux';

import App from './App';

test('App', () => { // delete ~
  useSelector.mockImplementation((selector) => selector({
    regions: [],
    categories: [],
    restaurants: [],
  })); // ~ delete
describe('App', () => { // add ~
  beforeEach(() => {
    useSelector.mockImplementation((selector) => selector({
      regions: [],
      categories: [],
      restaurants: [],
    }));
  });

  context('with path /', () => {
    it('renders the home page', () => {
      const { container } = render((
        <MemoryRouter initialEntries={['/']}>
          <App />
        </MemoryRouter>
      ));

      expect(container).toHaveTextContent('Home');
    });
  });

  context('with path /about', () => {
    it('renders the about page', () => {
      const { container } = render((
        <MemoryRouter initialEntries={['/about']}>
          <App />
        </MemoryRouter>
      )); // ~ add

  render(<App />); // delete
      expect(container).toHaveTextContent('서비스에'); // add ~
    });
  }); // ~ add
});
```

```jsx
// App.jsx
import {
  BrowserRouter, // delete
  Routes,
  Route,
} from 'react-router-dom';
import HomePage from './HomePage';
import AboutPage from './AboutPage';
import NotFoundPage from './NotFoundPage';
import RestaurantsPage from './RestaurantsPage';

export default function App() {
  return (
    <BrowserRouter> // delete ~
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/about" element={<AboutPage />} />
        <Route path="/restaurants" element={<RestaurantsPage />} />
        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </BrowserRouter> // ~ delete
    <Routes> // add ~
      <Route path="/" element={<HomePage />} />
      <Route path="/about" element={<AboutPage />} />
      <Route path="/restaurants" element={<RestaurantsPage />} />
      <Route path="*" element={<NotFoundPage />} />
    </Routes> // ~ add
  );
}
```

```jsx
// index.jsx
import ReactDOM from 'react-dom/client';

import { // add ~
  BrowserRouter,
} from 'react-router-dom'; // ~ add

import { Provider } from 'react-redux';

import App from './6주차강의/App';

import store from './6주차강의/store';

const app = ReactDOM.createRoot(document.getElementById('app'));

app.render(
  <Provider store={store}>
    <App /> // delete
    <BrowserRouter> // add ~
      <App />
    </BrowserRouter> // ~ add
  </Provider>,
);
```

```jsx
// HomePage.test.jsx
import { MemoryRouter } from 'react-router-dom'; // add

import { render } from '@testing-library/react';

import HomePage from './HomePage';

test('HomePage', () => {
  render(<HomePage />); // delete
  render(( // add ~
    <MemoryRouter>
      <HomePage />
    </MemoryRouter>
  )); // ~ add
});
```

```jsx
// HomePage.jsx
import { Link } from 'react-router-dom'; // add

export default function HomePage() {
  return (
    <div>
      <h1>Home</h1>
      <ul>
        <li><a href="/about">About</a></li> // delete ~
        <li><a href="/restaurants">Restaurants</a></li> // ~ delete
        <li><Link to="/about">About</Link></li> // add ~
        <li><Link to="/restaurants">Restaurants</Link></li> // ~ add
      </ul>
    </div>
  );
}
```

```jsx
// AboutPage.jsx
export default function AboutPage() {
  return (
    <p>About...</p> // delete
    <div> // add ~
      <h1>이 서비스에 대해서...</h1>
    </div> // ~ add
  );
}
```

## (11) App 컴포넌트의 path를 전달하는 액션을 리팩터링
```jsx
// App.test.jsx
import {
  MemoryRouter,
} from 'react-router-dom';

import { render } from '@testing-library/react';

import { useSelector } from 'react-redux';

import App from './App';

describe('App', () => {
  beforeEach(() => {
    useSelector.mockImplementation((selector) => selector({
      regions: [],
      categories: [],
      restaurants: [],
    }));
  });

  function renderApp({ path }) { // add ~
    return render((
      <MemoryRouter initialEntries={[path]}>
        <App />
      </MemoryRouter>
    ));
  } // ~ add

  context('with path /', () => {
    it('renders the home page', () => {
      const { container } = render(( // delete ~
        <MemoryRouter initialEntries={['/']}>
          <App />
        </MemoryRouter>
      )); // ~ delete
      const { container } = renderApp({ path: '/' }); // add

      expect(container).toHaveTextContent('Home');
    });
  });

  context('with path /about', () => {
    it('renders the about page', () => {
      const { container } = render(( // delete ~
        <MemoryRouter initialEntries={['/about']}>
          <App />
        </MemoryRouter>
      )); // ~ delete
      const { container } = renderApp({ path: '/about' }); // add

      expect(container).toHaveTextContent('서비스에');
    });
  });
});
```

## (12) '/restaurants' 경로를 확인하기 위한 액션을 테스트 및 구현
```jsx
// App.test.jsx
import {
  MemoryRouter,
} from 'react-router-dom';

import { render } from '@testing-library/react';

import { useSelector } from 'react-redux'; // delete
import { useDispatch, useSelector } from 'react-redux'; // add

import App from './App';

describe('App', () => {
  beforeEach(() => {
    const dispatch = jest.fn(); // add ~

    useDispatch.mockImplementation(() => dispatch); // ~ add

    useSelector.mockImplementation((selector) => selector({
      regions: [], // add ~
      regions: [
        { id: 1, name: '서울' },
      ], // ~ add
      categories: [],
      restaurants: [],
    }));
  });
  function renderApp({ path }) {
    return render((
      <MemoryRouter initialEntries={[path]}>
        <App />
      </MemoryRouter>
    ));
  }
  context('with path /', () => {
    it('renders the home page', () => {
      const { container } = renderApp({ path: '/' });
      expect(container).toHaveTextContent('Home');
    });
  });
  context('with path /about', () => {
    it('renders the about page', () => {
      const { container } = renderApp({ path: '/about' });
      expect(container).toHaveTextContent('서비스에');
    });
  });

  context('with path /restaurants', () => { // add ~
    it('renders the restaurants page', () => {
      const { container } = renderApp({ path: '/restaurants' });

      expect(container).toHaveTextContent('서울');
    });
  }); // ~ add
});
```

## (13) 잘못된 경로를 처리하기 위한 액션을 테스트 및 구현
```jsx
// App.test.jsx
import {
  MemoryRouter,
} from 'react-router-dom';

import { render } from '@testing-library/react';

import { useDispatch, useSelector } from 'react-redux';

import App from './App';

describe('App', () => {
  beforeEach(() => {
    const dispatch = jest.fn();

    useDispatch.mockImplementation(() => dispatch);

    useSelector.mockImplementation((selector) => selector({
      regions: [
        { id: 1, name: '서울' },
      ],
      categories: [],
      restaurants: [],
    }));
  });

  function renderApp({ path }) {
    return render((
      <MemoryRouter initialEntries={[path]}>
        <App />
      </MemoryRouter>
    ));
  }

  context('with path /', () => {
    it('renders the home page', () => {
      const { container } = renderApp({ path: '/' });

      expect(container).toHaveTextContent('Home');
    });
  });

  context('with path /about', () => {
    it('renders the about page', () => {
      const { container } = renderApp({ path: '/about' });

      expect(container).toHaveTextContent('서비스에');
    });
  });

  context('with path /restaurants', () => {
    it('renders the restaurants page', () => {
      const { container } = renderApp({ path: '/restaurants' });
      
      expect(container).toHaveTextContent('서울');
    });
  });

  context('with invalid path', () => { // add ~
    it('renders the not found page', () => {
      const { container } = renderApp({ path: '/xxx' });

      expect(container).toHaveTextContent('Not Found');
    });
  }); // ~ add
});
```

<!-- ① ② ③ ④ ⑤ ⑥ ⑦ ⑧ ⑨-->

<!-- ### 2. Link 넣기

```

유형 1: (설명어를 입력) : [gunhee's coding blog](https://gunhee-jeong.github.io/)
유형 2: (URL 자동연결) : <https://gunhee-jeong.github.io/>
유형 3: (동일 파일 내 '문단으로 이동') : [1. Header로 이동](###-1-header)

```

유형 1: (설명어를 입력) : [gunhee's coding blog](https://gunhee-jeong.github.io/)
유형 2: (URL 자동연결) : <https://gunhee-jeong.github.io/>
유형 3: (동일 파일 내 '문단으로 이동') : [1. Header로 이동](#1-header)
유형 3의 방법

1. 특수문자를 제거
2. 스페이스는 -로 바꾸고
3. 대문자는 소문자로!
   그래서 ### 1. Header -> #1-header

## Link: [google][https://www.google.com/]

### 3. 수평선

```

---

```

---

### 4. 라인 바꾸기

```

스페이스바를 2번 눌러주면 다음칸으로
이동할 수 있어요!

```

---

스페이스바를 2번 눌러주면
다음칸으로 이동할 수 있어요!

### 5. list 만들기

```

1. 1번
2. 2번
3. 3번

- 순서없는 list
  - 순서없는 list
    - 순서없는 list

```

1. 1번
2. 2번
3. 3번

- 순서없는 list
  - 순서없는 list
    - 순서없는 list

---

### 6. font 관련

```

**진하게** -> 볼드
_기울여서_ -> 이탤릭체
~~취소선~~ -> 취소선

<ul>밑줄넣기</ul> -> 밑줄
<span style="color:red">빨간 글씨</span> -> 글자색
이것이 `인라인` 입니다 -> 인라인 코드
```

**진하게** -> 볼드
_기울여서_ -> 이탤릭체
~~취소선~~ -> 취소선
<u>밑줄넣기</u> -> 밑줄
<span style="color:red">빨간 글씨</span>
이것이 `인라인` 입니다 -> 인라인 코드

---

### 7. 인용구문

```
> coding
>
> > JavaScript
> >
> > > 내가 프짱!
```

> coding
>
> > JavaScript
> >
> > > 내가 프짱!

---

### 8. 이미지 삽입

```
유형1: ('사이즈를 조절' -> HTML 태그 사용) : <img src="https://gunhee-jeong.github.io/assets/images/blogLogo.png" width="300" height="200">
유형2: (이미지 삽입 후 -> 링크 걸기)
[![이미지](https://gunhee-jeong.github.io/assets/images/blogLogo/blogLogo.png)](https://gunhee-jeong.github.io/)
```

유형1: ('사이즈를 조절' -> HTML 태그 사용) : <img src="https://gunhee-jeong.github.io/assets/images/blogLogo.png" width="300" height="200">
유형2: (이미지 삽입 후 -> 링크 걸기)
[![이미지](https://gunhee-jeong.github.io/assets/images/blogLogo.png)](https://gunhee-jeong.github.io/)

### 9. 표 만들기

```
||국어|영어|
| :--- | ---: | :--: |
|건희 | 100점 | 100점
|철수 | 100점 | 100점
```

|      |  국어 | 영어  |
| :--- | ----: | :---: |
| 건희 | 100점 | 100점 |
| 철수 | 100점 | 100점 |

> - header를 넣고 싶은 경우 ---을 사용하고 :을 이용하여 정렬에 사용함!

### 10. 토글 만들기

```
<details>
<summary>여기를 누르세요</summary>
<div markdown="1">
숨겨진 내용
</div>
</details>
```

<details>
<summary>여기를 누르세요</summary>
<div markdown="1">
숨겨진 내용
</div>
</details> -->
